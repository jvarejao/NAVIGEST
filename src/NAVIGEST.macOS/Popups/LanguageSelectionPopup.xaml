<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="NAVIGEST.macOS.Popups.LanguageSelectionPopup"
               Color="Transparent"
               CanBeDismissedByTappingOutsideOfPopup="True">

    <Border StrokeShape="RoundRectangle 20" 
            BackgroundColor="{DynamicResource CardBackgroundColor}"
            StrokeThickness="0"
            WidthRequest="300"
            Padding="20">
        <Border.Shadow>
            <Shadow Brush="Black" Opacity="0.2" Radius="20" Offset="0,10"/>
        </Border.Shadow>

        <VerticalStackLayout Spacing="15">
            <Label Text="Select Language / Idioma" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"
                   TextColor="{DynamicResource BodyTextColor}"/>

            <Button Text="🇵🇹 PT - Português" 
                    Clicked="OnPTClicked"
                    BackgroundColor="Transparent" 
                    TextColor="{DynamicResource BodyTextColor}"
                    HorizontalOptions="Fill"/>

            <Button Text="🇺🇸 EN - English" 
                    Clicked="OnENClicked"
                    BackgroundColor="Transparent" 
                    TextColor="{DynamicResource BodyTextColor}"
                    HorizontalOptions="Fill"/>

            <Button Text="🇫🇷 FR - Français" 
                    Clicked="OnFRClicked"
                    BackgroundColor="Transparent" 
                    TextColor="{DynamicResource BodyTextColor}"
                    HorizontalOptions="Fill"/>

            <Button Text="🇪🇸 ES - Español" 
                    Clicked="OnESClicked"
                    BackgroundColor="Transparent" 
                    TextColor="{DynamicResource BodyTextColor}"
                    HorizontalOptions="Fill"/>

            <Button Text="Cancel" 
                    Clicked="OnCancelClicked"
                    BackgroundColor="#8E8E93" 
                    TextColor="White"
                    CornerRadius="8"
                    Margin="0,10,0,0"/>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>
