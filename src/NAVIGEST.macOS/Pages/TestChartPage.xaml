<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:micro="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             x:Class="NAVIGEST.macOS.Pages.TestChartPage">
    <Grid RowDefinitions="Auto,*" Padding="20">
        <Label Text="Página de Teste (Microcharts)" FontSize="24" FontAttributes="Bold" Margin="0,0,0,20"/>
        
        <Border Grid.Row="1" StrokeShape="RoundRectangle 10" Padding="20" BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
            <micro:ChartView x:Name="TestChart"
                             HeightRequest="400"
                             HorizontalOptions="Fill"
                             VerticalOptions="Fill"/>
        </Border>
    </Grid>
</ContentView>