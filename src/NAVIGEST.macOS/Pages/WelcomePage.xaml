<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:beh="clr-namespace:NAVIGEST.macOS.Behaviors"
             xmlns:res="clr-namespace:NAVIGEST.macOS.Resources.Strings"
             x:Class="NAVIGEST.macOS.Pages.WelcomePage"
             Title="{x:Static res:AppResources.Welcome_Title}"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <Grid>
        <!-- Fundo -->
        <Image Source="background.jpg"
               Aspect="AspectFill"
               Opacity="0.2"
               IsVisible="True" />

        <!-- Custom Header (Language + Settings) -->
        <Grid VerticalOptions="Start" Padding="20" ColumnDefinitions="*, Auto, Auto" ColumnSpacing="10" ZIndex="999">
             <!-- Spacer -->
             
             <!-- Language Picker -->
             <Border Grid.Column="1" StrokeShape="RoundRectangle 12" BackgroundColor="#33000000" Padding="10,6" StrokeThickness="0">
                <HorizontalStackLayout Spacing="6">
                    <Label Text="{Binding CurrentLanguageFlag}" FontSize="18" VerticalOptions="Center"/>
                    <Label Text="{Binding CurrentLanguageCode}" TextColor="White" FontAttributes="Bold" VerticalOptions="Center"/>
                </HorizontalStackLayout>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ChangeLanguageCommand}"/>
                </Border.GestureRecognizers>
             </Border>

             <!-- Settings Button -->
             <Border Grid.Column="2" StrokeShape="RoundRectangle 12" BackgroundColor="#33000000" Padding="10,6" StrokeThickness="0">
                <Label Text="&#xF013;" FontFamily="FA7Solid" FontSize="20" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center"/>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnOpenConfigClicked"/>
                </Border.GestureRecognizers>
             </Border>
        </Grid>

        <!-- Conteúdo principal (mantido) -->
        <VerticalStackLayout Padding="30" Spacing="30" VerticalOptions="Center">

            <Image x:Name="LogoImage"
                   Source="yahcores.png"
                   HeightRequest="120"
                   HorizontalOptions="Center"
                   Opacity="0"
                   Scale="0.8"
                   Margin="0,20" />

            <Label x:Name="TituloLabel"
                   Text="{x:Static res:AppResources.Welcome_Subtitle}"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{DynamicResource PrimaryTextColor}"
                   Opacity="0" />

            <Label x:Name="DescricaoLabel"
                   Text="{x:Static res:AppResources.Welcome_Description}"
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   TextColor="{DynamicResource SecondaryTextColor}"
                   Opacity="0" />
           
        </VerticalStackLayout>

        <!-- OVERLAY DE LOADING (mantido) -->
        <Grid x:Name="LoadingOverlay"
              IsVisible="False"
              BackgroundColor="#80000000"
              InputTransparent="False">
            <Border Padding="24"
                    BackgroundColor="{AppThemeBinding Light=#F9FAFB, Dark=#1F2937}"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                <Border.Shadow>
                    <Shadow Opacity="0.25" Radius="12" Offset="0,6" />
                </Border.Shadow>
                <VerticalStackLayout Spacing="12" HorizontalOptions="Center" VerticalOptions="Center">
                    <ActivityIndicator IsRunning="True"
                                       WidthRequest="32" HeightRequest="32"/>
                    <Label x:Name="LoadingLabel"
                           Text="{x:Static res:AppResources.Welcome_Connecting}"
                           FontSize="14"
                           TextColor="{AppThemeBinding Light=#111827, Dark=White}"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <!-- TOAST (mantido, com cor) -->
        <Grid Padding="16" InputTransparent="True"
              HorizontalOptions="Fill" VerticalOptions="End">
            <Border x:Name="StatusToast"
                    IsVisible="False"
                    Opacity="0"
                    StrokeShape="RoundRectangle 12"
                    Padding="14,10"
                    BackgroundColor="#8022C55E">
                <Label x:Name="StatusText"
                       Text="mensagem"
                       FontSize="14"
                       TextColor="White"
                       HorizontalTextAlignment="Center"/>
            </Border>
        </Grid>

        <!-- PAINEL: escolher empresa ativa -->
        <Grid x:Name="CompanyPickerPanel"
              IsVisible="False"
              BackgroundColor="#80000000"
              InputTransparent="False">
            <Border Padding="20"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    BackgroundColor="{AppThemeBinding Light=#F9FAFB, Dark=#111827}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Opacity="0.25" Radius="12" Offset="0,6" />
                </Border.Shadow>

                <VerticalStackLayout Spacing="12"
                    x:Name="CompanyListContainer"
                    WidthRequest="{OnPlatform Default=600, Android=250, iOS=250}"
                    MaximumWidthRequest="{OnPlatform Default=600, Android=250, iOS=250}">
                    <Label Text="{x:Static res:AppResources.Welcome_ChooseCompany}"
                           FontSize="20"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="{AppThemeBinding Light=#111827, Dark=White}"/>

                    <CollectionView x:Name="CompanyList"
                                    SelectionMode="Single"
                                    SelectionChanged="OnCompanySelected"
                                    HorizontalOptions="Fill">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="Auto,*"
                                      Padding="10"
                                      BackgroundColor="{AppThemeBinding Light=#E0F2FE, Dark=#1F2937}"
                                      HorizontalOptions="Fill">
                                    <Grid.Behaviors>
                                    </Grid.Behaviors>
                                    <Grid.GestureRecognizers>
                                        <!-- FIX: passa o item como CommandParameter -->
                                        <TapGestureRecognizer NumberOfTapsRequired="1"
                                                              Tapped="OnCompanyTapped"
                                                              CommandParameter="{Binding .}"/>
                                    </Grid.GestureRecognizers>

                                    <!-- LOGO CIRCULAR COM BORDA COLORIDA -->
                                    <Image Source="{Binding LogoSrc}"
                                           Aspect="AspectFill"                                            
                                           WidthRequest="56" HeightRequest="56" Margin="2">
                                        <Image.Clip>
                                            <EllipseGeometry Center="28,28" RadiusX="28" RadiusY="28" />
                                        </Image.Clip>
                                    </Image>

                                    <!-- NOME DA EMPRESA -->
                                    <Label Grid.Column="1"
                                           Text="{Binding Empresa}"
                                           FontSize="16"
                                           VerticalTextAlignment="Center"
                                           TextColor="{AppThemeBinding Light=#111827, Dark=White}"
                                           Margin="14,0,0,0"
                                           LineBreakMode="TailTruncation"
                                           HorizontalOptions="Fill"/>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
