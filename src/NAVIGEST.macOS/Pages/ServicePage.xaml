<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NAVIGEST.macOS.Pages.ServicePage"
             x:Name="ServicePageRoot"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:NAVIGEST.macOS.Models"
             xmlns:behaviors="clr-namespace:NAVIGEST.macOS.Behaviors"
             Title="Serviços"
             BackgroundColor="{DynamicResource PageBackgroundColor}"
             Loaded="OnPageLoaded">

    <Grid RowDefinitions="Auto, *" 
          Padding="24,24,24,0" 
          RowSpacing="16"
          MaximumWidthRequest="1200"
          HorizontalOptions="Center">
        
            <!-- HEADER: Search & Add -->
            <Grid ColumnDefinitions="*, Auto" ColumnSpacing="16">
                <!-- Search -->
                <Border StrokeShape="RoundRectangle 10" 
                        BackgroundColor="{DynamicResource CardBackgroundColor}"
                        StrokeThickness="0"
                        Padding="8,0"
                        HeightRequest="44"
                        VerticalOptions="Center"
                        MinimumWidthRequest="400">
                    <Grid ColumnDefinitions="*,Auto">
                        <Entry Placeholder="Pesquisar por nº, cliente ou estado…" 
                               Text="{Binding Filter}" 
                               BackgroundColor="Transparent"
                               TextColor="{DynamicResource BodyTextColor}"
                               HeightRequest="44"
                               VerticalOptions="Center"
                               Style="{StaticResource AppleEntry}"
                               Margin="0"/>
                        
                        <Button Grid.Column="1"
                                Text="✕"
                                TextColor="{DynamicResource EntryPlaceholderColor}"
                                BackgroundColor="Transparent"
                                Command="{Binding ClearSearchCommand}"
                                IsVisible="{Binding Filter, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                                WidthRequest="30"
                                HeightRequest="30"
                                Padding="0"
                                VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <!-- Add Button -->
                <Button Grid.Column="1" 
                        Text="Novo Serviço" 
                        Command="{Binding NewCommand}"
                        BackgroundColor="#0A84FF" 
                        TextColor="White" 
                        FontAttributes="Bold" 
                        CornerRadius="12" 
                        HeightRequest="44"
                        VerticalOptions="Center"
                        Padding="20,0">
                    <Button.Behaviors>
                        <behaviors:ButtonMicroAnimationsBehavior />
                    </Button.Behaviors>
                </Button>
            </Grid>

        <!-- Lista -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding Orders}" SelectionMode="None" ItemSizingStrategy="MeasureFirstItem" Margin="0,0,0,20">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:OrderInfoModel">
                    <Border Padding="16" Margin="0,0,0,12" StrokeThickness="0" StrokeShape="RoundRectangle 12" BackgroundColor="{DynamicResource CardBackgroundColor}">
                        <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="16">
                            <!-- Info -->
                            <VerticalStackLayout Grid.Column="0" Spacing="4" VerticalOptions="Center">
                                <Label Text="{Binding CustomerName}" FontAttributes="Bold" FontSize="16" TextColor="{DynamicResource ListRowTextColor}" LineBreakMode="TailTruncation"/>
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding OrderNo}" FontSize="14" Opacity="0.8" TextColor="{DynamicResource ListRowTextColor}"/>
                                    <Label Text="|" FontSize="14" Opacity="0.4" TextColor="{DynamicResource ListRowTextColor}"/>
                                    <Label Text="{Binding OrderStatus}" FontSize="14" Opacity="0.8" TextColor="{DynamicResource ListRowTextColor}"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Total -->
                            <Label Grid.Column="1" 
                                   Text="{Binding TotalAmount, StringFormat='{}{0:N2} €'}" 
                                   VerticalOptions="Center" 
                                   FontAttributes="Bold"
                                   FontSize="16"
                                   TextColor="{DynamicResource ListRowTextColor}"/>

                            <!-- Actions -->
                            <HorizontalStackLayout Grid.Column="2" Spacing="8" VerticalOptions="Center">
                                <Button Text="Editar" 
                                        Command="{Binding Source={x:Reference ServicePageRoot}, Path=BindingContext.EditCommand}"
                                        CommandParameter="{Binding .}"
                                        FontSize="13"
                                        HeightRequest="36"
                                        Padding="12,0"
                                        BackgroundColor="#007AFF"
                                        TextColor="White"
                                        CornerRadius="8"/>
                                <Button Text="Eliminar" 
                                        Command="{Binding Source={x:Reference ServicePageRoot}, Path=BindingContext.DeleteCommand}"
                                        CommandParameter="{Binding .}"
                                        FontSize="13"
                                        HeightRequest="36"
                                        Padding="12,0"
                                        BackgroundColor="#FF3B30"
                                        TextColor="White"
                                        CornerRadius="8"/>
                            </HorizontalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                    <Label Text="Sem serviços para mostrar" FontSize="18" FontAttributes="Bold" TextColor="{DynamicResource BodyTextColor}" HorizontalOptions="Center"/>
                    <Label Text="Verifique a ligação à base de dados ou crie um novo serviço." FontSize="14" TextColor="{DynamicResource BodyTextColor}" Opacity="0.7" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <!-- Overlay -->
        <Grid Grid.RowSpan="2" IsVisible="{Binding IsBusy}" BackgroundColor="#80000000" ZIndex="999">
            <ActivityIndicator IsRunning="{Binding IsBusy}" VerticalOptions="Center" HorizontalOptions="Center" Color="White"/>
        </Grid>
    </Grid>
</ContentPage>
